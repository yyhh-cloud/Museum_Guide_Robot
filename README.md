# 🏛️ 博物馆智能导览机器人 (Museum Guide Robot)

基于 **STM32 双板架构** 与 **K210 视觉模块** 的智能导览系统，支持 AprilTag 视觉导航。
采用分层控制设计：上层负责逻辑决策与视觉交互，底层负责运动控制，实现了“听得懂指令、找得着展品、停得稳位置”的自动导览功能。

## ✨ 功能亮点
* **👀 视觉导航**：利用 K210 识别 AprilTag (二维码)，实时解算坐标与距离。
* **🧠 智能决策**：C10B 板载状态机，支持按键切换目标 (蒙娜丽莎 / 星空)。
* **🔄 自动变道**：识别到路口 Tag 0 时，通过“协议欺骗”技术控制底盘转向。
* **📏 精确停车**：基于焦距常数 (F-Constant) 算法，实现厘米级定点停车。

## 🛠️ 硬件清单 (System Hardware)
整个系统由三部分核心硬件组成：
* **🧠 决策/感知层**：STM32F103RCT6 (**C10B 传感器板**)
    * *负责逻辑判断、处理 K210 数据、读取超声波与陀螺仪。*
* **👀 视觉感知层**：K210 (**MaixBit / CanMV**)
    * *负责运行 Python 脚本，识别路标与黑线。*
* **🦵 运动控制层**：STM32F103RCT6 (**C50C 驱动板**) + 麦克纳姆轮底盘
    * *负责接收上层指令，执行 PID 电机闭环控制 (本层使用标准固件，未修改)。*
* **📡 传感器**：MPU6050 6轴陀螺仪 + HC-SR04 超声波测距

## 📂 文件与架构说明 (Files & Architecture)

本项目代码分为三个模块，分别对应上述硬件：

### 1. 视觉层 (Vision)
* **`linefollow_k210.py`**
    * 运行于 K210 开发板。
    * **功能**：同时运行 AprilTag 识别与巡线算法。当识别到路标时，优先发送 Tag ID 和距离；未识别时发送巡线偏差。

### 2. 决策层 (Logic - C10B) 🌟 *核心修改部分*
* **`main.c`** (位于 STM32_C10B 工程)
    * **功能**：系统的“大脑”。实现了导航状态机、按键扫描、转向冷却逻辑以及与底层的通信。
* **`k210.c / k210.h`**
    * **功能**：通信协议栈。负责解析 K210 发来的自定义数据包。

### 3. 驱动层 (Driver - C50C)
* **(Standard Firmware)**
    * 运行于 C50C 底层驱动板。
    * **说明**：本项目复用了官方标准的 PID 运动控制固件。它只负责“听话”——接收 C10B 发来的 `X轴速度`、`Y轴速度` 和 `使能信号` 来驱动电机，**无需修改代码**。

## 📥 下载完整工程
本项目核心代码已直接上传。
如需完整的 Keil 工程文件（包含库函数和驱动），请下载仓库中的 `Museum_Guide_Robot.zip` 文件，解压后即可编译运行。


---
## 🚀 快速开始
1.  **烧录**：将 `main.c` 编译后的固件烧录至 **C10B**，将 `main.py` 存入 **K210**。
2.  **上电**：开启底盘电源，OLED 显示 `Mode: Patrol`。
3.  **交互**：
    * 单击按键 -> 屏幕显示 `Go->Lisa` -> 小车自动前往 1 号展品。
    * 双击按键 -> 屏幕显示 `Go->Star` -> 小车自动前往 2 号展品。

---

*Created by yyhh - 2025*
